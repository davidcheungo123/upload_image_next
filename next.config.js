/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // rewrites: [
  //   {source: '/*', destination: '/api/*'}
  // ],
}

module.exports = nextConfig
